# packages/shared

Shared types, utilities, and constants used across the monorepo.

Intended for future cross-cutting concerns such as:

- Shared TypeScript types / Zod schemas used by both frontend and a future TypeScript SDK
- Common utility functions
- OpenAPI-generated client types

Add contents here as the project grows. Keep this package framework-agnostic.
